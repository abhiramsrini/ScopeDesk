<Window x:Class="ScopeDesk.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ScopeDesk" Height="620" Width="980" Background="{DynamicResource SurfaceBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource PrimaryBrush}">
            <DockPanel Margin="20,0">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <Rectangle Width="32" Height="32" Fill="{DynamicResource AccentBrush}" RadiusX="6" RadiusY="6" />
                    <TextBlock Text="ScopeDesk" FontSize="22" FontWeight="Bold" Foreground="White" Margin="12,0,0,0" />
                </StackPanel>
                <TextBlock DockPanel.Dock="Right"
                           VerticalAlignment="Center"
                           Foreground="White"
                           FontWeight="SemiBold"
                           Text="{Binding StatusMessage}"
                           TextTrimming="CharacterEllipsis" />
            </DockPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Row="0" Grid.ColumnSpan="2" Background="{DynamicResource CardBrush}" Padding="16" CornerRadius="8" Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Vertical" Grid.Column="0" Grid.RowSpan="2">
                            <TextBlock Text="Oscilloscope IP" Style="{StaticResource LabelText}" />
                            <TextBox Text="{Binding IpAddress, UpdateSourceTrigger=PropertyChanged}" 
                                     Style="{StaticResource InputTextBox}" />
                        </StackPanel>

                        <Button Grid.Column="1" Grid.Row="0"
                                Content="Connect"
                                Style="{StaticResource PrimaryButton}"
                                Command="{Binding ConnectCommand}"
                                IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBoolConverter}}" />

                        <Button Grid.Column="2" Grid.Row="0"
                                Content="Disconnect"
                                Style="{StaticResource SecondaryButton}"
                                Command="{Binding DisconnectCommand}"
                                IsEnabled="{Binding IsConnected}" />

                        <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2" VerticalAlignment="Center" Margin="0,6,0,0">
                            <TextBlock Text="Status:" Style="{StaticResource LabelText}" />
                            <TextBlock Text="{Binding ConnectionStatusText}" FontWeight="SemiBold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource TertiaryBrush}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                                <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Grid.Column="0" Background="{DynamicResource CardBrush}" Padding="16" CornerRadius="8" Margin="0,0,16,16">
                    <StackPanel>
                        <TextBlock Text="Channel" Style="{StaticResource LabelText}" Margin="0,0,0,8" />
                        <ComboBox ItemsSource="{Binding ChannelOptions}"
                                  SelectedItem="{Binding SelectedChannel}"
                                  Style="{StaticResource InputComboBox}"
                                  DisplayMemberPath="DisplayName" />
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Grid.Column="1" Background="{DynamicResource CardBrush}" Padding="16" CornerRadius="8" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Measurements" Style="{StaticResource LabelText}" Margin="0,0,0,8" />
                        <ListBox ItemsSource="{Binding MeasurementOptions}" BorderThickness="0" Background="Transparent">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding DisplayName}"
                                              IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                              Foreground="{DynamicResource ForegroundBrush}"
                                              Margin="0,2" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Border>

                <Border Grid.Row="2" Grid.ColumnSpan="2" Background="{DynamicResource CardBrush}" Padding="16" CornerRadius="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <Button Content="Fetch Measurements"
                                    Style="{StaticResource PrimaryButton}"
                                    Command="{Binding FetchMeasurementsCommand}"
                                    IsEnabled="{Binding IsConnected}" />
                            <Button Content="Open Logs Folder"
                                    Style="{StaticResource GhostButton}"
                                    Command="{Binding OpenLogsCommand}" />
                        </StackPanel>

                        <TextBlock Grid.Row="1" Text="Results" Style="{StaticResource LabelText}" Margin="0,6,0,0" />
                        <DataGrid Grid.Row="2"
                                  AutoGenerateColumns="False"
                                  ItemsSource="{Binding Results}"
                                  HeadersVisibility="Column"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  IsReadOnly="True"
                                  Background="Transparent"
                                  Foreground="{DynamicResource ForegroundBrush}"
                                  BorderBrush="{DynamicResource SecondaryBrush}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" Width="160" />
                                <DataGridTextColumn Header="Channel" Binding="{Binding Channel}" Width="80" />
                                <DataGridTextColumn Header="Measurement" Binding="{Binding Measurement}" Width="*" />
                                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="180" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>

        <Border Grid.Row="2" Background="{DynamicResource PrimaryBrush}" Padding="12,6">
            <TextBlock Text="{Binding FooterMessage}" Foreground="White" />
        </Border>
    </Grid>
</Window>
